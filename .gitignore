# Foundry build + cache
out/
cache/
broadcast/
build/

# forge doc html build output (the markdown sources under docs/ stay tracked)
docs/book/

# dependencies (pinned via .gitmodules / package.json, never vendored)
lib/
node_modules/
sdk/dist/
*.tsbuildinfo

# secrets + local env
.env
.env.*
*.env
!.env.example

# editor / os
.DS_Store
*.log
